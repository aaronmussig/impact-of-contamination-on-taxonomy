# FastTree Marker Split

This workflow takes all failed bacterial genomes from the GUNC set:

1. Split the marker genes into two sets (putative contamination, non-contamination)
2. Create a tree, root, decorate